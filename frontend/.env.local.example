NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Optional - enables the live map on /track/:awb and /admin/dashboard.
# Leave blank to fall back to a plain pin list.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
